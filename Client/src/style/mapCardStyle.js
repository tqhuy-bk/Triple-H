import { makeStyles } from '@material-ui/core';

const mapCardStyles = makeStyles(
  theme => ({
    container: {
      height: '100%',
      marginInline: 20,
      marginTop: 50
    }
}), {index: 1});

export default mapCardStyles;
